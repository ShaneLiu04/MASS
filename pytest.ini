[pytest]
# Skip integration tests (network-dependent) by default for fast CI feedback
addopts = -m "not integration" -v

testpaths = tests

markers =
    integration: Tests depending on external network/services (akshare, crawlers, real LLM, etc.)
    slow: Tests taking longer than 5 seconds
